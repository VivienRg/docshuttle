{
  "name": "cf-docs-portal",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "setup":   "node scripts/setup.js",
    "secrets": "node scripts/push-secrets.js",
    "build":   "node scripts/build.js",
    "dev":     "wrangler dev",
    "deploy":  "npm run build && wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "^3"
  }
}
